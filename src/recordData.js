/**
Funcion para gravar los datos de una nueva orden de compra
  @param: idSpreadSheet, id de la hoja de calculo correspondiente a local que esta emitiendo la orden de compra
  @return: true si el registro fue exitoso. false si hay algun error
**/

function recordOrder(idSpreadSheet) {
  
  
  return true
}